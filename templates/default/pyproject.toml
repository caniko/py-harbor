[project]
name = "minimal"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = []

[project.optional-dependencies]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
